@extends('layouts.app')
@section('content')
<div class="mb-6 flex items-center justify-between"><div><h1 class="text-3xl font-bold">Employee Profile</h1><p class="text-slate-500">{{ $employee->employee_id }}</p></div><div class="flex gap-3"><a href="{{ route('employees.edit', $employee) }}" class="rounded-lg bg-slate-900 px-4 py-2 text-white">Edit</a><form method="POST" action="{{ route('employees.destroy', $employee) }}" onsubmit="return confirm('Delete this employee?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-300 px-4 py-2 text-red-600">Delete</button></form></div></div>
<div class="grid gap-6 md:grid-cols-3"><div class="rounded-xl bg-white p-6 text-center shadow-sm"><div class="mx-auto flex h-28 w-28 items-center justify-center rounded-full bg-slate-200 text-3xl font-bold">{{ strtoupper(substr($employee->first_name, 0, 1) . substr($employee->last_name, 0, 1)) }}</div><h2 class="mt-4 text-xl font-bold">{{ $employee->full_name }}</h2><p class="text-slate-500">{{ $employee->position ?? 'No position specified' }}</p><p class="mt-3 inline-block rounded-full bg-slate-100 px-3 py-1 text-sm">{{ $employee->employment_status }}</p></div><div class="rounded-xl bg-white p-6 shadow-sm md:col-span-2"><h2 class="mb-4 text-xl font-bold">Employee Information</h2><dl class="grid gap-4 md:grid-cols-2"><div><dt class="text-sm text-slate-500">Employee ID</dt><dd>{{ $employee->employee_id }}</dd></div><div><dt class="text-sm text-slate-500">Department</dt><dd>{{ $employee->department ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Email</dt><dd>{{ $employee->email ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Contact Number</dt><dd>{{ $employee->contact_number ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Birthdate</dt><dd>{{ $employee->birthdate?->format('F d, Y') ?? '—' }}</dd></div><div><dt class="text-sm text-slate-500">Date Hired</dt><dd>{{ $employee->date_hired?->format('F d, Y') ?? '—' }}</dd></div><div class="md:col-span-2"><dt class="text-sm text-slate-500">Address</dt><dd>{{ $employee->address ?? '—' }}</dd></div></dl></div></div>
@endsection
